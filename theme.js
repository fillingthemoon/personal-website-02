import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  colors: {
    black: '#333',
    blue: '#0078f7',
  },
  fonts: {
    body: 'Open Sans',
    heading: 'Open Sans',
  }
})

export default theme